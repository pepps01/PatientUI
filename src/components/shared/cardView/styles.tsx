import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    shadowOpacity: 0.5,
    shadowOffset: {
      width: 0,
      height: 3,
    },
    elevation: 10,
  },
});

export default styles;
